exports['default'] = {
  plugins: (api) => {
    /*
    if you want to use plugins in your application, inculde them here
    IE:

    return {
      'myPlugin': { path: __dirname + '/../node_modules/myPlugin' }
    }

    You can also toggle on or off sections of a plugin to include (default true for all sections):
    IE:

    return {
      'myPlugin': {
        path: __dirname + '/../node_modules/myPlugin',
        actions: true,
        tasks: true,
        initializers: true,
        servers: true,
        public: true
      }
    }
    */

    return {}
  }
}
