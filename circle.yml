machine:
  services:
    - redis
  environment:
    FAKEREDIS: false
    maxMemoryAlloted: 500
  node:
    version: 8
test:
  override:
    - nvm install 4 && nvm use 4 && npm install && npm test
    - nvm install 6 && nvm use 6 && npm install && npm test
    - nvm install 8 && nvm use 8 && npm install && npm test
